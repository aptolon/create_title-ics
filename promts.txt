python -m PyInstaller --onefile composition.py титул_шаблон_пример.docx
напиши программу  на python Tkinter в которой сначала открытвается страница выберите файл, после того как выбирается файл exel открывается страница сведение о проекте с полями : заказчик, наименование проекта и фон.
первые два поля это текст, третье это картинка. если в файле есть во второй строке эти поля, то должны выводится они, в фоне естественно хранится полный путь к файлу. В фоне должна быть кнопочка выбрать файл, а в форме выводится картинка.
ТАк же должна быть кнопка сохранить, которая сохраняет соотвественно все в нужные яйчейки exel

когда загружаем картинку то должен быть формат jpg или png

сделай чтобы поля с заказчиком и поля и с исполнителем были больше и сделай так чтобы слева были все поля и кнопки а справа высвечивалась только картинка

нужно сделать двустраничную форму. НА первой странице "информация о проекте", а на второй, должны быть такие поля: они берутся автоматически со второго листа, названия в первой строке второго листа, так же должно быть название и поле на самой вторйо странице

на второй странице должны быть такие же отступы как и на первой, а так же такое жн разделение на лево и право. в правой части должен быть список томов 

сначала должны быть такие поля Номер тома	Обозначение	Наименование	Примечание	pdf-файл
а затем уже все что есть начиная с столбца N

исправь pdf-файл, это не должно быть полем, это кнопка внизу выбрать pdf файл


добавь кнопку добавить, которая будет добавлять значения из полей в первую строку которая свободна на второй странице


нужно создать список Combobox в котором будут строки со второго листа. Обозначение - Номер тома и Наименование, при добавлении список должен 

нужно чтобы на второй странице список и все кнопки были слева, а все поля справа

на второй странице сначала идет список, потом кнопки добавить, удалить, редактировать и очистить поля в одну строку, потом надпись путь файла пдф, а под ней выбрать файл pdf, внизу слева назад к информации о проекте


на второй странице при выборе из списка нужно заполнить поля соответсвующими значениями

сделай так чтобы значения в exel заменялись на новые при  условии что выбран элемент из списка

нужно удалить выбранную строку в excel

нужно сделать так чтобы файл выбирался только при том что если выбран элемент в combobox, путь файла должен записываться в excel таблицу в столбец E

label должен обновляться на новыйээ

проверка на то что не должно быть двух томов с одинаковыми названиями

если проект пустой то нужно создать лист Сведения о проекте в котором будут в первой строке Заказчик	Наименование проекта	Фон
а так же лист Состав проекта в котором будут значения Номер тома	Обозначение	Наименование	Примечание	pdf-файл	Разработал	Проверил	ГИП	Н.Контроль


на первой странице должна быть галочка преобразовать в заглавные под наименованием проекта, так же 

добавь под наименованием проекта галочку преобразовать в заглавные, а так же сделай поле наименованием проекта больше по высоте
замени все tk.Entry на tk.Text и измени методы для сохранения в excel и подкачки из excel


напиши функцию которая будет располагать поверх картинки текст из self.customer.get(), self.project_name.get(), self.project_code.get(), self.ceo.get(), self.project_engineer.get(), self.city.get(), self.year_of_development.get()

вверху заказчик - self.customer.get()
чуть ниже self.project_code.get() с переносами строк

далее Проектная документация, далее раздел и так далее



должен открываться титул_шаблон_пример, заменяться [Заказчик], [НАИМЕНОВАНИЕ ПРОЕКТА] [Шифр проекта] [Город] и [Год разработки] на соответсвующие значения, позже эта страница должна конвертироваться в test.png и сохраняться

напиши функцию, которая открывает титул_шаблон_пример.dox, заменяет поля         
    tk.Label(left_frame, text="Заказчик:").pack(anchor="w")
        customer_entry = tk.Entry(left_frame, textvariable=self.customer, width=60)

        tk.Label(left_frame, text="Наименование проекта:").pack(anchor="w")
        self.project_name_entry = tk.Entry(left_frame, textvariable=self.project_name, width=60)

        tk.Label(left_frame, text="Шифр проекта:").pack(anchor="w")
        project_code_entry = tk.Entry(left_frame, textvariable=self.project_code, width=60)

        tk.Label(left_frame, text="Генеральный директор:").pack(anchor="w")
        ceo_entry = tk.Entry(left_frame, textvariable=self.ceo, width=60)

        tk.Label(left_frame, text="Главный инженер проекта:").pack(anchor="w")
        project_engineer_entry = tk.Entry(left_frame, textvariable=self.project_engineer, width=60)

        tk.Label(left_frame, text="Город:").pack(anchor="w")
        city_entry = tk.Entry(left_frame, textvariable=self.city, width=60)

        tk.Label(left_frame, text="Год разработки:").pack(anchor="w")
        year_of_development_entry = tk.Entry(left_frame, textvariable=self.year_of_development, width=60)
        
на их значения если эти поля есть в виде [Заказчик] [Шифр проекта] в документе

добавь номер раздела в виде селект бокс, маленьким размером, следом наименование раздела это должно быть объединено и добавлено в 1 строку